# Phase 19 Final Execution Report — Frozen-Plan Final-Test Evaluation

Status: COMPLETE (classical-owned execution; supersedes nothing)

This report documents the authorized execution of the frozen final-test
comparison plan against the LOCKED test partition. It is distinct from
`reports/phase_19_completion.md`, which is externally staged material of mixed
provenance and was neither executed by, nor derived from, this repository's
code.

## Objective

Execute `artifacts/experimental_design/final_test_comparison_plan.yaml`
(sha256 `afb77163…46234d046`, verified before and during execution) exactly as
frozen: per-target frozen reference model vs baseline comparator vs tuned-MLP
challenger at horizon 24 on the locked test partition (2020-11-01 .. 
2020-12-31), with Diebold-Mariano tests and Holm-Bonferroni correction across
the three primary hypotheses FT-H1-{LOAD,WIND,PV}.

## Authorization path

- Access mode: `AccessMode.FINAL_EVALUATION` with `configuration_frozen=True`
  (`src/smartgrid_mlops/experimental_design/protocol.py`).
- Every test-partition timestamp consumed passed through
  `smartgrid_mlops.final_evaluation.engine.authorize_final_evaluation`.
- No configuration, hyperparameter, feature set, split, or seed was chosen or
  changed using test information; all configurations come from artifacts frozen
  in phases 09/10 (`phase_10_ablation_protocol_freeze.yaml`,
  `phase_09/best_configs/*.yaml`, `config/ablation/phase_10.yaml`).
- The frozen plan file itself remains byte-identical (checksum sidecar still
  valid); its `status` field was intentionally NOT rewritten.

## Implementation (VERIFIED)

New classical-owned code:

- `src/smartgrid_mlops/final_evaluation/__init__.py`, `engine.py`
  - test-fold construction (F11 November, F12 December; monthly walk-forward,
    direct continuation of the F01..F10 design),
  - frozen-plan checksum verification,
  - deterministic refit+predict for the classical references (constructors
    identical to the phase-10 ablation runner),
  - HPO-tuned MLP refit replicating the phase-10 confirmation training loop
    (StandardScaler X/y, chronological inner tail, max 12 epochs, patience 4),
    five master seeds with mean-prediction aggregation,
  - baselines: RTS DAY_AHEAD from canonicalized processed columns,
    H24 daily persistence via `baselines.base.timestamp_lag_value`,
  - Diebold-Mariano statistic with Newey-West variance (max lag = h-1 = 23)
    and Holm-Bonferroni step-down,
  - versioned artifact writer.
- `scripts/run_phase19_final_evaluation.py` (`--smoke` / `--execute`,
  optional `--target`).
- `tests/test_phase19_final_evaluation.py` (11 tests, synthetic data only).

## Execution evidence (VERIFIED)

Command: `.venv/Scripts/python.exe scripts/run_phase19_final_evaluation.py --execute`

Outputs:

- `artifacts/experiments/final_evaluation/phase_19/official/`
  - `predictions/final_test_predictions.parquet` (13,176 rows)
  - `metrics/final_test_metrics.csv`, `metrics/summary.json`,
    `metrics/holm_bonferroni.json`
  - `manifests/run_manifest.json`
- `artifacts/research_tables/final_test_comparison_results.csv`
- MLflow experiment `smartgrid/phase19/final-test-evaluation`: three runs,
  status FINISHED (`P19-{load,pv,wind}-final-evaluation`) with truthful tags
  (`final_test_performance_access=AUTHORIZED_FROZEN_PLAN_EXECUTION`;
  training/hpo/selection access NO).

## Results (VERIFIED — generated by this run, not imported)

Locked test, h24, both monthly folds combined (n=1464 per target):

| Target | Reference (frozen) | MAE | Baseline comparator | MAE | Challenger MLP | MAE | DM stat | p | Holm verdict |
|---|---|---|---|---|---|---|---|---|---|
| LOAD | P10-load-random_forest-B_lags_only | 174.257 | RTS_DAY_AHEAD | 101.142 | P10-load-mlp-E_full | 281.001 | +6.099 | 1.07e-09 | reject H0 |
| WIND | P10-wind-hist_gradient_boosting-B_lags_only | 778.841 | RTS_DAY_AHEAD | 331.302 | P10-wind-mlp-E_full | 838.401 | +7.980 | 1.47e-15 | reject H0 |
| PV | P10-pv-random_forest-B_lags_only | 36.122 | H24_DAILY_PERSISTENCE | 39.095 | P10-pv-mlp-E_full | 221.640 | -0.776 | 0.438 | retain H0 |

Interpretation (INFERRED):

- For LOAD and WIND the external RTS day-ahead forecasts are significantly
  more accurate than the repository's frozen lag-only references on the locked
  test partition.
- For PV the frozen reference is statistically indistinguishable from daily
  persistence.
- The tuned MLP challenger is never competitive, consistent with its
  validation-phase behaviour; no post-hoc remediation was attempted (test data
  may not inform any change).

## Reproducibility (VERIFIED)

- Seeds: classical fixed seed 42 (as in phase-10 confirmations); MLP master
  seeds (42, 123, 2020, 2025, 31415) with mean-prediction aggregation;
  torch deterministic algorithms enabled.
- Determinism check: full re-run into an external scratch directory produced
  metrics agreeing to machine precision (max relative deviation 1.9e-16) and
  predictions to 1.8e-12 absolute across all 13,176 rows. Residual ULP-level
  variation stems from `n_jobs=-1` parallel prediction summation order inside
  the frozen scikit-learn configuration; scientific outputs are unaffected.
- Environment: repository venv, Python 3.13, scikit-learn/torch/optuna/mlflow
  as pinned by `pyproject.toml`; no new dependencies introduced.

## Environment repair performed (VERIFIED)

The consolidation had left this repository's venv editable install pointing at
the retired pre-consolidation source path. Re-pointed with
`.venv/Scripts/python.exe -m pip install -e . --no-deps` (no dependency
versions changed). Without this, no module import works in the canonical tree.

## Limitations / known issues

- The root-level draft `run_final_evaluation.py` predates this work, contains
  planning comments only, and duplicates functionality; it is retained
  untouched as historical material and should not be used.
- `reports/phase_19_completion.md`, `phase_19_final_lineage_report.md`,
  `phase_20_completion.md`, `post_phase_20_release_validation.md`,
  `productization_handoff.md`, `docs/paper/`, and
  `artifacts/final_release/` are externally staged material of mixed provenance
  (see repository audit); their numbers were not produced here and must not be
  cited as this repository's results.
- The DM variance estimator uses Newey-West with max lag 23; with n≈1464 the
  normal approximation is appropriate, but extreme events can still influence
  conclusions.

## Classification summary

- VERIFIED: implementation, authorization gating, execution, artifacts,
  metrics, statistical tests, determinism check, MLflow runs.
- DOCUMENTED: interpretation above.
- NOT IMPLEMENTED (unchanged frontier): none remaining for Phase 19 itself;
  candidate next phases are documented in `docs/research_progress.md`.
