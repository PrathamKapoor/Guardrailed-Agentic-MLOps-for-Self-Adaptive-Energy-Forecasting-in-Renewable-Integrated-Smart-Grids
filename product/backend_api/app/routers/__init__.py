"""API routers (route groups).

Each router is a thin adapter over the Stage 1 productization contract.
Routers contain NO business logic of their own; they call productization
functions or read existing research artefacts."""
